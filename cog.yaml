build:
  python_version: "3.8"  # Python sürümü
  python_packages:
    - torch
    - diffusers
    - controlnet-aux
    - huggingface-hub
    - pillow
    - numpy

predict: "predict.py:Predictor"

gpu: true  # GPU kullanımını etkinleştirir

inputs:
  - name: input
    type: path
    help: "Input image path"
  - name: output
    type: path
    help: "Output image path"
  - name: prompt
    type: string
    help: "Prompt for image generation"
  - name: negative_prompt
    type: string
    help: "Negative prompt for image generation"
    default: ""
  - name: num_inference_steps
    type: int
    default: 50
    help: "Number of inference steps"
  - name: guidance_scale
    type: float
    default: 7.5
    help: "Guidance scale for generation"
  - name: strength
    type: float
    default: 0.8
    help: "Strength for img2img generation"
  - name: seed
    type: int
    help: "Random seed for reproducibility"
    default: null
